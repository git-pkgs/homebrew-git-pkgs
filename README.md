# Homebrew Tap for git-pkgs

This is the official Homebrew tap for [git-pkgs](https://github.com/git-pkgs/git-pkgs).

## Installation

```bash
brew tap git-pkgs/git-pkgs
brew install git-pkgs
```

## Updating

```bash
brew update
brew upgrade git-pkgs
```
